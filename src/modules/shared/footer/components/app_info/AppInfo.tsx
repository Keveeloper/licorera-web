import { Box } from "@mui/material";

const AppInfo = () => {
    return(
        <Box sx={styles.appInfoContainer}>
            App info
        </Box>
    );

}

const styles = {
    appInfoContainer: {
        width: '33.3%',
    }
}

export default AppInfo;